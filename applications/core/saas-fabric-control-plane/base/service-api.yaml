apiVersion: v1
kind: Service
metadata:
  name: saas-fabric-control-plane
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: saas-fabric-control-plane
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
