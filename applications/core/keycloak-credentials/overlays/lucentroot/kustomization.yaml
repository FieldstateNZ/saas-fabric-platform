apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Identical in every environment: a generated credential has nothing
# environment-specific about it. The overlay exists so an environment that
# sources this from elsewhere has somewhere to say so.
resources:
  - ../../base
