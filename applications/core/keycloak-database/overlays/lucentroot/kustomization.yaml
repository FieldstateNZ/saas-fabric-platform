apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patches:
  - target:
      kind: Cluster
      name: keycloak-db
    patch: |-
      - op: add
        path: /spec/storage/storageClass
        value: local-path
