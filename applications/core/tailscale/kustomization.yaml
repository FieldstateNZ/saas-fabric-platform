apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# The operator plane is enabled per environment rather than by every
# environment, so it is its own kustomize root: an environment opts in by
# listing this directory. See environments/README.md.
resources:
  - application.yaml
