apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# The store is identical in every environment: same in-cluster OpenBao, same
# auth method, same mount. An environment overlay exists so that one with a
# different secrets backend has somewhere to say so.
resources:
  - ../../base
