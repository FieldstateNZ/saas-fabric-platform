apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# A disposable seal is an environment-specific lifecycle choice, not a component
# every environment runs. LucentRoot lists this directory; production seals
# against durable key material instead, so listing it there would be wrong
# rather than merely unnecessary.
resources:
  - application.yaml
