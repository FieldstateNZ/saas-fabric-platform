apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# LucentRoot only. Production seals against durable external key material, which
# is a different mechanism with different recovery semantics -- see
# environments/production/config/openbao.yaml.
resources:
  - ../../base
