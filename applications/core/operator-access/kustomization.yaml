apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Part of the operator plane, enabled per environment alongside
# applications/core/tailscale. See environments/README.md.
resources:
  - application.yaml
