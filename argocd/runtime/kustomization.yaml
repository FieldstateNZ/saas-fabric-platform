apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Argo CD behaviour this platform depends on. Applied at bootstrap so it is
# active before the first wave-ordered sync, then reconciled by Argo CD so it
# cannot drift.
resources:
  - application-health.yaml
  - server-insecure.yaml
