apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# saas-fabric-platform itself lives in bootstrap/project.yaml: it must exist
# before Argo CD manages anything, and an Application must not be able to widen
# the project that constrains it.
resources:
  - saas-fabric-catalogue.yaml
