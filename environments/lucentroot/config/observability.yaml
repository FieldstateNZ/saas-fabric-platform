# LucentRoot: no telemetry backend yet. The debug exporter configured in the
# shared values is left in place, so signals are visible in the collector's own
# logs. Replacing it here is a single-file change.
replicaCount: 1

resources:
  requests:
    cpu: 25m
    memory: 96Mi
  limits:
    memory: 256Mi
