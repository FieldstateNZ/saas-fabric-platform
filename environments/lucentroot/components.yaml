# What LucentRoot is asked to run.
#
# Machine-managed by SaaS Fabric Platform Management. Everything below this
# header is written by Fabric in a deterministic layout, so a hand edit
# survives as values and not as formatting. Editing it by hand is the
# break-glass path and is expected to keep working.
#
# Desired state only. What versions are *available* is discovered from
# registries and what is *running* comes from the cluster; neither is recorded
# here.
#
#   artifact:     what this component is published as, and its provenance
#   channel:      the release stream newer versions are drawn from
#   update:       automatic | manual | locked
#   desired:      the version this environment is asked to run
#   hold:         pauses automatic advancement without changing policy
#   pinnedIn:     platform-owned locations where the version is written, each
#                 naming the renderer that writes it
#
# See environments/README.md for the complete contract.
---
schemaVersion: 2
environment: lucentroot
managedRoots:
- applications/
components:
  saas-fabric:
    artifact:
      type: oci
      sourceRevision: 69dc8a7fc561bbb21b2c57e13a5929fc3c1b2017
      images:
        console:
          repository: ghcr.io/fieldstatenz/saas-fabric-control-plane-ui
          digest: sha256:30c2048cd0fd51d1b85a8427f192faed0fa540750c9973fc8d9cefe308172b12
        controlPlane:
          repository: ghcr.io/fieldstatenz/saas-fabric-control-plane
          digest: sha256:c545a7523cf059bd62fef0192cb1173d5dd18c20b7ae4d31ded1000ac5d31b36
        runtime:
          repository: ghcr.io/fieldstatenz/saas-fabric
          digest: sha256:90955fade480e6c228f30fb5b36ea066c92711da9ea822336c9c8bb9e23afa39
    channel: preview
    update: automatic
    desired:
      version: 0.3.0-preview.11
    pinnedIn:
    - renderer: kustomize-image
      path: applications/core/saas-fabric-control-plane/overlays/lucentroot/kustomization.yaml
      image: console
    - renderer: kustomize-image
      path: applications/core/saas-fabric-control-plane/overlays/lucentroot/kustomization.yaml
      image: controlPlane
    - renderer: kustomize-image
      path: applications/core/saas-fabric/overlays/lucentroot/kustomization.yaml
      image: runtime
    hold:
      reason: paused
      since: '2026-09-17T05:27:42Z'
      note: 'Waiting for the control-plane audience to land with the next advance; see #33'
